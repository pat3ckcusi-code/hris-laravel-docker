; Allow uploads up to 128 MB - required for database backup restore.
; nginx client_max_body_size is already set to 64M in nginx.conf;
; raise that too if backups grow beyond 64 MB.
upload_max_filesize = 128M
post_max_size       = 128M
memory_limit        = 256M
